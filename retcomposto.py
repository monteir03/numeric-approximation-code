import math;

a = 1
b = 2
def f(x):
    return math.e**x;

h=(b-a)/4

t=a
soma=0

for i in range(n):
    soma+=h*f(h)
    t+=h

print(soma)
#ou multiplicar por h no final

def derivada(x):
    return math.e**x

if (derivada(a)>derivada(b))
    max=derivada(a)
else
    max=derivada(b)
#erro

erro=(b-a)/2*h*max(#1º derivada maxima)
